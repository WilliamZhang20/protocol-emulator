# Tiny Tapeout project information
project:
  title:        "SRAM-Programmed Protocol Emulator"      # Project title
  author:       "William Zhang"      # Your name
  discord:      ""      # Your discord username, for communication and automatically assigning you a Tapeout role (optional)
  description:  "A deterministic programmable engine for bit-level hardware protocols"      # One line description of what your project does
  language:     "Verilog" # other examples include SystemVerilog, Amaranth, VHDL, etc
  clock_hz:     50000000       # 20 ns target period in src/config.json

  # How many tiles your design occupies? A single tile is about 167x108 uM.
  tiles: "6x4"          # Jane Street protocol-emulator competition allocation

  # Your top module name must start with "tt_um_". Make it unique by including your github username:
  top_module:  "tt_um_protocol_emulator"

  # List your project's source files here.
  # Source files must be in ./src and you must list each source file separately, one per line.
  # Don't forget to also update `PROJECT_SOURCES` in test/Makefile.
  source_files:
    - "project.v"
    - "protocol_emulator_core.v"
    - "instruction_decoder.v"
    - "program_memory.v"
    - "register_file.v"
    - "timer_counter.v"
    - "bit_transfer_engine.v"
    - "gpio_datapath.v"
    - "byte_fifo.v"
    - "host_interface.v"

# The pinout of your project. Leave unused pins blank. DO NOT delete or add any pins.
# This section is for the datasheet/website. Use descriptive names (e.g., RX, TX, MOSI, SCL, SEG_A, etc.).
pinout:
  # Inputs
  ui[0]: "host_in[0]"
  ui[1]: "host_in[1]"
  ui[2]: "host_in[2]"
  ui[3]: "host_in[3]"
  ui[4]: "host_in[4]"
  ui[5]: "host_in[5]"
  ui[6]: "host_in[6]"
  ui[7]: "host_in[7]"

  # Outputs
  uo[0]: "host_out[0]"
  uo[1]: "host_out[1]"
  uo[2]: "host_out[2]"
  uo[3]: "host_out[3]"
  uo[4]: "host_out[4]"
  uo[5]: "host_out[5]"
  uo[6]: "host_out[6]"
  uo[7]: "host_out[7]"

  # Bidirectional pins
  uio[0]: "protocol_gpio[0]"
  uio[1]: "protocol_gpio[1]"
  uio[2]: "protocol_gpio[2]"
  uio[3]: "protocol_gpio[3]"
  uio[4]: "protocol_gpio[4]"
  uio[5]: "protocol_gpio[5]"
  uio[6]: "protocol_gpio[6]"
  uio[7]: "protocol_gpio[7]"

# Do not change!
yaml_version: 6
